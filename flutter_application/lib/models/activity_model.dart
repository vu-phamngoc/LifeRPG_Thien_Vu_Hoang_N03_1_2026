class ActivityModel {
  final String title;
  final String description;
  final DateTime createdAt;

  const ActivityModel({
    required this.title,
    required this.description,
    required this.createdAt,
  });
}
